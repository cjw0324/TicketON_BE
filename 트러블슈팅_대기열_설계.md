# 대기열 시스템 설계 트러블슈팅 기록

## 1. Tomcat maxConnections를 조절한 이유

### 배경

서버 환경은 EC2 프리티어(t2.micro, vCPU 1개, RAM 1GB)였다. Spring Boot Tomcat의 기본 `maxConnections` 값은 8,192이지만, 이 환경에서는 그대로 사용할 수 없었다.

### 산정 근거

**RAM 기준**

SSE 연결 1개당 약 5~8KB를 소비한다(SseConnection 객체 + SseEmitter 내부 버퍼 + Tomcat AsyncContext). 사용 가능한 JVM 힙이 약 400MB이므로 이론상 최대 5만 개가 가능하지만, 안전 한계는 3,000~5,000개 수준이다.

**CPU 기준 (더 현실적인 병목)**

t2.micro의 지속 CPU 성능은 약 10%에 불과하다. `QueueInfoScheduler`가 매 1초마다 전체 대기자 N명에게 Redis 조회(2N+1회)를 수행하고 SSE를 전송하므로, 대기자가 1,000명을 넘어서면 1 vCPU 환경에서 빡빡해진다.

**OS 파일 디스크립터 한도**

기본값이 1,024~4,096으로 maxConnections보다 먼저 막힐 수 있어, `/etc/security/limits.conf`에서 4,096 이상으로 조정이 필요하다.

### 결론

위 분석을 바탕으로 t2.micro 기준 현실적인 권장값인 **300~500으로 maxConnections를 설정**했다.

```yaml
server:
  tomcat:
    max-connections: 500
    threads:
      max: 50        # 기본 200 → 메모리 절약
      min-spare: 10
    accept-count: 50
```

---

## 2. 장애의 원인

### 증상

maxConnections를 500으로 설정한 상태에서 500명을 초과하는 동시 접속이 발생하자 **신규 연결이 거부되는 응답 실패 장애**가 발생했다.

### 원인 메커니즘

SSE는 HTTP 연결을 끊지 않고 유지하는 방식이다. 일반 HTTP 요청은 수십 ms 만에 소켓을 반납하지만, SSE 연결은 대기가 끝날 때까지 TCP 소켓(maxConnections 슬롯)을 계속 점유한다. 결과적으로 슬롯이 포화 상태에 이르고, 초과 요청은 거부된다.

### 흔히 오해하는 부분

**SSE가 스레드 풀을 고갈시킨다는 설명은 부정확하다.**

Spring MVC의 `SseEmitter`는 Servlet AsyncContext를 사용하기 때문에, SSE 연결이 수립되는 순간 Tomcat 워커 스레드는 풀로 반납된다. 실제로 점유되는 자원은 스레드가 아닌 **TCP 소켓(maxConnections 슬롯)**이다.

| 잘못된 표현 | 정확한 표현 |
|---|---|
| SSE → WAS 스레드 풀 고갈 | SSE → TCP 소켓(maxConnections) 포화 |
| SSE 연결이 스레드를 점유 | SSE 연결이 소켓 슬롯을 점유 |

### 부가적 장애 요인: CPU 스파이크

수천 명이 동시에 SSE 연결을 요청하면 TLS 핸드셰이크, JWT 검증, Redis 토큰 조회가 동시에 폭발적으로 발생해 Tomcat 워커 스레드가 풀 가동되고 CPU가 순간적으로 치솟는다. 연결 이후에도 `QueueInfoScheduler`의 매초 Redis 반복 조회(2N+1회/초)로 지속적인 부하가 유지된다.

---

## 3. SSE의 사용 목적

이 프로젝트에서 SSE는 두 곳에서 서로 다른 목적으로 사용된다.

| 위치 | 목적 | 연결 수명 | Heartbeat |
|---|---|---|---|
| `main-server` (`NotificationEmitterService`) | 결제 완료, 시스템 알림 등 일반 알림 전송 | 장기 (타임아웃 1시간) | 있음 (5초마다) |
| `queue-server` (`SseEmitterService`) | 대기열 순번 실시간 전송 + **사용자 이탈 감지** | 단기 (입장 허가까지) | 없음 |

### queue-server에서 SSE를 선택한 핵심 이유: 이탈 감지

사용자가 브라우저를 닫거나 네트워크가 끊기면 SSE 연결의 `onError()` 또는 `onCompletion()` 콜백이 트리거되어, 해당 사용자를 대기열(Redis ZSet)에서 즉시 제거한다.

단방향 폴링 방식으로는 이 이탈을 서버 측에서 즉각 감지하기 어렵다. 폴링에서는 마지막 요청 이후 일정 시간이 지나야 비로소 이탈로 판단할 수 있어, 그 사이에 이탈한 사용자가 대기열 순번을 점유하는 문제가 생긴다.

또한 SSE는 `QueueInfoScheduler`가 매 1초마다 전체 대기자에게 현재 순번(rank)을 push하는 채널로도 활용된다. **이탈 감지 요구사항 때문에 폴링 방식으로의 전환이 불가능했고, SSE를 유지하는 방향으로 결정**했다.

---

## 4. 멀티모듈 구조를 채택한 이유

### 문제 인식

단일 `queue-server` 인스턴스에서는 maxConnections 한계(500)와 스케줄러 부하(매 1초 × 2N Redis 조회)로 인해 수용 한계가 명확했다. SSE 유지를 결정했으므로, 단일 인스턴스의 한계를 **수평 확장(scale-out)**으로 해결해야 했다.

### 구체적 설계

SSE는 특정 서버 인스턴스에 고정(sticky)된다. 즉, 사용자의 SSE 연결은 최초 접속한 인스턴스에만 존재한다. 따라서 입장 허가 메시지를 보낼 때, **SSE가 연결된 바로 그 인스턴스로 정확히 라우팅**해야 한다.

이를 위해 다음 구조가 도입되었다.

**1. 대기 등록 시 `instanceId` 저장**

사용자가 대기열에 진입할 때 어느 queue-server 인스턴스와 SSE를 맺고 있는지를 Redis Hash에 기록한다. 단일 인스턴스라면 이 정보가 불필요하지만, 수평 확장 구조에서는 라우팅의 핵심 키가 된다.

**2. `instanceId` 기반 메시지 라우팅**

`message-dispatcher`가 입장 허가 메시지를 발행할 때, 사용자의 `instanceId`를 조회해 해당 인스턴스의 Redis Stream에만 메시지를 전달한다.

**3. 인스턴스별 독립 Consumer Group**

Redis Stream의 Consumer Group 이름을 `DISPATCH:{instanceId}` 형태로 구성하여 각 인스턴스가 자신에게 라우팅된 메시지만 소비한다.

### 서비스 모듈 구성

| 모듈 | 역할 |
|---|---|
| `common` | 공통 도메인, 유틸리티 |
| `main-server` | 일반 API, 결제, 일반 알림(SSE) |
| `message-dispatcher` | waiting → entry 승격 및 instanceId 기반 라우팅 |
| `queue-server` | SSE 기반 대기열 처리 (수평 확장 대상) |
| `user` | 사용자 인증/인가 |

### 전체 흐름 요약

```
하드웨어 한계 (t2.micro)
  → maxConnections 500 설정
  → 500명 초과 시 응답 실패 장애 발생
  → SSE 이탈 감지 요구사항으로 폴링 전환 불가 → SSE 유지 결정
  → 수평 확장 구조 설계
  → instanceId 저장 → message-dispatcher 라우팅 → 인스턴스별 Consumer Group
  → 멀티모듈 구조 채택
```